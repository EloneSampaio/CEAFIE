
<div class="control-group pull-right">
    <div class="controls centered">
        <select class="span6 m-wrap" name="genero">
            <option value="000">-Buscar Por-</option>
            <option value="Masculino">ABERTO</option>
            <option value="Femenino">CONFIRMADO</option>
        </select>                                    
    </div>
</div>
<br />
<table  class="table table-striped table-bordered">   
    <thead>
        <tr>

            <th>Nª BI</th> 
            <th>Nome</th>
            <th>CURSO</th>
            <th>MODÚLO</th>
        </tr>
    </thead>

    <tbody>

        <?php if (isset($this->dados) && !empty($this->dados)): ?>
            <?php foreach ($this->dados as $dados): ?>
                <tr>
                    <td><?php print $dados->getAluno()->getPessoa()->getBi(); ?></td>
                    <td><?php print $dados->getAluno()->getPessoa()->getNome(); ?></td>
                    <td><?php print $dados->getModulo()->getCurso()->getNome();?></td>
                    <td><?php print $dados->getModulo()->getNome(); ?></td>
                    <td><a target="__BLANK" href="<?php echo URL . "diploma" . DS1 . "gerar/" . $dados->getAluno()->getId(); ?>">DIPLOMA</a></td>
                </tr>

                <?php
            endforeach;
        endif;
        ?>

    </tbody>
</table>



