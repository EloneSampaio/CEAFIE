<?php echo $this->menu; ?>
<div id="content" style="margin-left: 120px;">
    <!-- validation -->
    <div class="row-fluid">
        <!-- block -->
        <div class="block">

      <div style="position:absolute;
                 top: 250px;
                 left: 600px;">
          <input type="number" name="ano" id="ano" data-required="1"  placeholder="Ano" maxlength="4"class="span2 m-wrap" />

                <select id="curso" >
                    <option value="">- Curso -</option>

                </select>

              
            </div>

            <table  class="table table-striped table-bordered" id="tabela">   
                <thead>
                    <tr class="odd gradeA">

                        <th>Nª BI</th> 
                        <th>Nome</th>
                        <th>CURSO</th>
                        <th>Ano de Conclusão</th>
                        <th>IMPRIMIR</th>
                    </tr>
                </thead>

                <tbody>

                    <?php if (isset($this->dados) && !empty($this->dados)): ?>
                        <?php foreach ($this->dados as $dados): ?>
                            <tr>
                                <td><?php print $dados->getAluno()->getPessoa()->getBi(); ?></td>
                                <td><?php print $dados->getAluno()->getPessoa()->getNome(); ?></td>
                                <td><?php print $dados->getModulo()->getCurso()->getNome(); ?></td>
                                <td><?php print date("Y",  strtotime($dados->getData())); ?></td>
                                <td><a target="__BLANK" href="<?php echo URL . "certificado" . DS1 . "gerar/" . $dados->getAluno()->getId(); ?>">Certificado</a></td>
                            </tr>

                            <?php
                        endforeach;
                    endif;
                    ?>

                </tbody>
            </table>
        </div>
    </div>


