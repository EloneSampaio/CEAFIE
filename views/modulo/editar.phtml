<div class="span10 " id="content" style="margin-left: 220px;">
    <!-- validation -->
    <div class="row-fluid">
        <!-- block -->
        <div class="block">

            <table  class="table table-striped table-bordered">   
                <thead>
                    <tr>

                        <th>Nome</th>
                        <th>Curso</th>

                    </tr>
                </thead>

                <tbody>

                    <?php if (isset($this->dados) && !empty($this->dados)): ?>
                        <?php foreach ($this->dados as $dados): ?>
                            <tr>

                                <td><?php print $dados->getNome(); ?></td>
                                <td><?php print $dados->getCurso()->getNome(); ?></td>
                                <td><a href="#" id="curso1" rel="<?php echo $dados->getId(); ?>"><i class="icon-edit"></i></a></td>
                            </tr>

                            <?php
                        endforeach;
                    endif;
                    ?>

                </tbody>
            </table>
        </div>
    </div>
</div>





